import {NavBar} from "../../shared/NavBar";
import React from 'react';

export const Compare = () => {
    return (
        <div>
            <NavBar/>
            COMPARE PENDING!!
        </div>
    );
};