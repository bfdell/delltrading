import {NavBar} from "../../shared/NavBar";
import React from 'react';

export const Portfolio = () => {
    return (
        <div>
            <NavBar/>
            PORTFOLIO PENDING!!
        </div>
    );
};